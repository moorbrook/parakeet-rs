audio/
raw.log
# `asr_diff` baseline. Machine-local: the fixtures come from macOS `say`,
# so the transcripts depend on the local TTS voice and OS version.
# Record your own with `asr_diff --record`.
transcripts.json
*.csv
!cleanup-backends.csv
